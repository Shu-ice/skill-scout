import { Text, View } from "react-native";

export default function Dashboard() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>ここは成績（レーダーチャート）が表示される画面です。</Text>
    </View>
  );
} 